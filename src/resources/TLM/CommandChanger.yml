# 指令配置
Commands:
  # 配置序号（不可重复）
  test:
    # 输入命令
    Input: '/kit'
    # 替换命令
    Replace: '/tlm kit'
    # 替换模式（省略默认全部）
    # ---------- #
    # CONSOLE = 后台
    # PLAYER = 玩家
    # ---------- #
    ReplaceMode: 'PLAYER'